version: '3.4'

services:
  bibliotecadigital:
    image: ${DOCKER_REGISTRY-}bibliotecadigital
    build:
      context: .
      dockerfile: Biblioteca digital/Dockerfile
    depends_on: 
       - "SqlServer"

  SqlServer:
    image: mcr.microsoft.com/mssql/server:2022-latest #mcr.microsoft.com/azure-sql-edge
    ports:
      - "1433:1433"
    environment:
      - SA_PASSWORD=LibCmos.145! #OdiN.7072$
      - ACCEPT_EULA=Y 
